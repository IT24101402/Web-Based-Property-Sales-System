# =======================
# Server (optional)
# =======================
server.port=8080

# =======================
# MySQL datasource
# =======================
spring.datasource.url=jdbc:mysql://localhost:3306/property_sales?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=Olu2005K1#@78
# driver is auto-detected in Boot 3.x, no need to set it

# =======================
# JPA / Hibernate
# =======================
# dev-only; use 'validate' or Flyway in prod
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# recommended to avoid lazy-loading leaks
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# =======================
# CORS (frontends allowed)
# =======================
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# =======================
# Logging (optional)
# =======================
logging.level.root=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
